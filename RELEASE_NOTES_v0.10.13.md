# v0.10.13 修复说明

## 修复：锚点模式尾部特征序列校验误报

v0.10.12 的正式线段账本与真实提交时间已经正确，但
`detect_segments_from_anchor()` 只在线段完整形成时保存本轮扫描得到的
特征元素和特征分型。最后一个尚未形成完整线段的尾部扫描结果会被丢弃，
导致 `validate_feature_sequence_coverage()` 误报：

```text
FEATURE_SEQUENCE_TAIL_NOT_SCANNED
```

本版本改为：每次 `_scan_segment()` 完成后，无论是否形成完整线段，都先保存
本轮特征元素、特征分型和诊断，再决定是否提交线段。正式结构算法、几何结果、
稳定笔边界和提交时间均未改变。

## 回归验证

- 真实 BTCUSDT 1h、500 根：26 笔、19 稳定笔、2 条正式线段，校验问题 0。
- 真实数据 50→500 根逐前缀扫描：170 个已形成正式线段的前缀，误报 0。
- Demo 5000 根：56 条正式线段，正式账本校验问题 0。
- 右边界稳定性：候选笔正常回撤 175 次，稳定笔和正式结构回撤 0。
- 左边界窗口验证：失败 0。
- 全量测试：107 passed，1 skipped（可选 `czsc` 依赖未安装）。
