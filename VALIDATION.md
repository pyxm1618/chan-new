# 5000 根实时数据版 v0.10.16 验证报告

## v0.10.16 身份与证据安全验证

```bash
PYTHONPATH=src python scripts/validate_identity_safety.py
```

结果：

```text
错误品种 MACD 锚点：拒绝
错误周期 MACD 锚点：拒绝
锚点与首根 K 存在断档：拒绝
输入 K 线内部断档：不得标记 exact
跨品种 SegmentEvidence：拒绝
按 segment_index 提交时间映射：拒绝
买卖点确认时间篡改：检出
空正式线段链：安全返回
两笔短尾：保留审计特征元素
seed 509 尾笔迁移误报：0
失败：0
```

全量测试：123 passed，1 skipped。跳过项仅为未安装可选 `czsc` 依赖的参考差分测试。

## v0.10.15 正式结构活性验证

```bash
PYTHONPATH=src python scripts/validate_structure_liveness.py --seeds 1000 --bars 300
```

结果：

```text
随机种子：1000
每个种子 K 线：300
发生共享端点迁移的种子：252
稳定锚点丢失：0
稳定笔或正式线段非前缀变化：0
正式结构提交滞后：0
失败：0
```

5000 根 DEMO 的稳定边界现为：完整笔 338、正式线段几何稳定笔 328、候选尾笔 10。确认线段所使用的后续证据笔保存在 `SegmentEvidence` 中，但不会再错误写入 `stable_strokes`。

## v0.10.13 尾部特征序列覆盖验证

- 真实 BTCUSDT 1h 500 根：正式线段账本校验问题 0。
- 真实数据 50→500 根逐前缀扫描：170 个有效前缀，
  `FEATURE_SEQUENCE_TAIL_NOT_SCANNED` 误报 0。
- Demo 5000 根：56 条正式线段，正式账本校验问题 0。
- 全量测试：107 passed，1 skipped。

```bash
PYTHONPATH=src python scripts/validate_feature_tail_coverage.py
```


## 0. 正式线段校验与提交时间验证

```bash
PYTHONPATH=src python scripts/validate_segment_commits.py --bars 5000
```

结果：

```text
正式线段：56
稳定笔：328
正式账本校验问题：0
缺少 committed_at：0
提交时间逆序：0
提交原始 K 位置逆序：0
批量结果与逐根首次提交时刻不一致：0
首次正式提交：2026-01-01T14:04:59.999000+00:00
最后正式提交：2026-01-18T02:39:59.999000+00:00
```

这里的“正式提交时间”不是线段端点时间，也不是特征序列确认笔的端点时间，
而是 `StructureState` 实际将线段追加到 append-only 正式账本时，当前原始 K
的收盘时间。正式买卖点的 `confirmed_at_dt` 优先使用该时间。

### 测试套件

```text
107 passed, 1 skipped
```

## 1. 左右边界必须同时通过

v0.10.11 将验证拆为两个独立问题：

- 右边界：新 K 到来后，正式笔、线段和中枢不得回撤；
- 左边界：任意有限窗口没有可信起点时，不得发布可能错相位的正式首段。

### 右边界逐根扫描

```bash
PYTHONPATH=src python scripts/validate_structure_stability.py --bars 5000
```

结果：

```text
候选笔链回撤：175 次（只允许发生在 provisional 层）
稳定笔回撤：0
正式线段回撤：0
正式笔中枢消失/右边界缩回：0
正式线段中枢消失/右边界缩回：0
最终正式线段：56
```

该脚本显式使用 `left_boundary_anchored=True`，因为 DEMO 生成器从已知真实起点
开始；它只验证右侧提交账本。

### 左边界窗口与持久化锚点扫描

```bash
PYTHONPATH=src python scripts/validate_window_stability.py --bars 5000
```

结果：

```text
可信完整历史参考：338 笔、56 正式线段、46 笔中枢、1 线段中枢
无锚点截断窗口：19 个，正式结构错误输出 0
持久化端点锚点窗口：20 个，正式线段非参考后缀 0
其中形成正式笔中枢的锚点窗口：14 个，中枢非参考连续子序列 0
锚点已被裁掉：1 个，未解析却输出正式结构 0
总失败：0
```

无锚点测试不是通过跳过第一条或前 N 条结构来“对齐”，而是强制：

```python
result.segments == ()
result.central_zones == ()
result.segment_central_zones == ()
result.trading_points == ()
result.unresolved_prefix_segments == result.detected_segments
```

持久化锚点测试则要求锚点后的正式线段严格等于完整历史正式线段的后缀；正式
中枢只能是完整历史结果的连续子序列。锚点匹配失败时必须安全关闭。

### 测试套件

```text
101 passed, 1 skipped
```

## 2. 历史验证内容（v0.10.10 及基础数据层）


## v0.10.10 正式结构单调性验证

本版不再把 `strokes[:-1]` 或任意固定尾部长度称为稳定前缀，而是逐根维护
`StructureState` 的提交账本。验证对象明确分成：

```text
detected_strokes / detected_segments：允许变化的候选检测层
stable_strokes / segments：只追加的正式层
provisional_strokes / provisional_segments：两者之间的可回撤尾部
```

运行命令：

```bash
PYTHONPATH=src python scripts/validate_structure_stability.py --bars 5000
```

实际输出摘要：

```text
原始 K 前缀：5000 个
候选笔链发生非前缀变化：175 次
最终完整笔链：338
最终稳定笔：328
最终候选笔：10
最终正式线段：56
最终候选线段：1

稳定笔回撤：0
正式线段回撤：0
正式笔中枢消失：0
正式笔中枢右边界缩回：0
正式线段中枢消失：0
正式线段中枢右边界缩回：0
```

候选层的 175 次变化证明不能靠固定裁尾得到稳定前缀；正式层的六类异常均为
零，才是本次修复的通过条件。测试还单独覆盖了 125→126、175→176、
235→236、378→379、403→404、633→634、810→811、990→991。

下面保留原有数据源、特征序列和展示层验证内容。涉及“已确认笔”的旧表述，
在 v0.10.10 中应理解为候选检测层历史统计；正式下游只消费 `stable_strokes`。

## 验证目标

本次改动同时影响数据获取、刷新、缓存、结构确认和绘图，因此按以下顺序验证：

```text
分页获取与时间序列
→ 已收盘 / 当前 K 隔离
→ 增量刷新与窗口滚动
→ 5000 根全结构重算
→ 笔 / 线段 / 中枢 / 买卖点不变量
→ 独立参考差分
→ 最后检查实线与虚线图层
```

图层代码只在数据层测试通过后接入。

## 1. 5000 根分页

现货回归模拟了交易所的 5001 行结果：5000 根已收盘 K 加 1 根未来收盘的当前 K。

验证结果：

```text
分页请求：6 次
最终已收盘 K：5000
重复 open_time：0
当前 K 混入已收盘层：0
首尾时间：与模拟源完全一致
```

分页测试不是简单伪造最终列表，而是让客户端按 `endTime` 向前翻页，覆盖实际生产循环。

## 2. 快照与增量刷新

验证了：

- 快照始终保持 5000 根已收盘 K + 最多 1 根当前 K；
- 同一根当前 K 的 OHLCV 修订会替换旧值，不会新增重复时间；
- 新 K 出现后，旧当前 K 进入已收盘窗口，最老历史 K 滚出；
- 当前 K 变化而没有新收盘 K 时，复用同一个确认分析对象；
- 新 K 收盘时，确认分析对象必须重新计算；
- 页面暂停超过完整历史窗口时，强制完整重载，不拼接陈旧头部；
- 失败时由 UI 保留上一份快照。

## 3. 防止当前 K 污染正式结构

同一批数据分别计算：

```text
confirmed = 仅已收盘 K
live      = 已收盘 K + 当前 K
```

测试断言：

```text
confirmed.raw_bars == 单独分析已收盘 K
confirmed.strokes  == 单独分析已收盘 K
confirmed.segments == 单独分析已收盘 K
live.raw_bars[-1]  == 当前未收盘 K
```

因此当前 K 即使瞬间创新高或新低，也只能改变虚线候选，不能提前确认笔、线段、中枢或买卖点。

## 4. 5000 根 5m 全流程压力回归

使用确定性 DEMO 数据：

```text
已收盘原始 K：5000
当前 K：1
无包含 K：3721
分型：851
完整笔链：338
稳定笔：328
候选笔：10
直接检测线段：57
正式线段：56
候选线段：1
正式笔中枢：仅使用 stable_strokes
正式线段中枢：仅使用 segments
买卖点：0
时间缺口：0
```

单次完整分析参考耗时约 `3.1s`（当前容器，非性能承诺）。

不变量检查：

```text
笔链问题：0
线段链问题：0
笔中枢问题：0
线段中枢问题：0
买卖点问题：0
```

标准特征序列独立参考：

```text
线段：57 / 57 一致
确认依据：57 / 57 一致
特征元素：659
特征分型：88
特征序列扫描位置：337 / 337
尾部覆盖问题：0
```

## 5. 首段有限窗口边界修复

上传分支修复了另一项独立问题：旧版首段选择会在某个较早起点首次形成完整
候选后立即返回，没有检查候选起终点是否仍为候选内部同类端点的真实极值。
5000 根回归中的旧候选 `0 → 7` 为向下段，但候选内部存在更高顶，因此该
起点不能作为窗口内自洽的首段起点。

合并后的首段流程：

```text
扫描所有满足首三笔重叠的可能起点
→ 每个起点独立运行完整标准特征序列
→ 排除段内起点/终点极值被破坏的完整候选
→ 按最早完成终点选择
→ 同终点时取更极端起点；同价时取更晚起点
→ 候选起点之前的笔进入未解析窗口前缀
```

5000 根结果：

```text
旧首段：0 → 7，向下，109.080684 → 90.023831（内部更高顶 110.165599）
新首段：1 → 4，向上， 90.211353 → 110.165599
未解析窗口前缀：1 笔
排除极值违规候选：228
截断窗口后续链：40 / 40 一致
```

新增独立保护：

- 首条已确认线段必须通过段内同类端点极值检查；
- 被排除的完整候选不能从未完成候选回退分支重新进入结果；
- 1000 组随机笔链中，生产实现与独立事件回放逐条一致；
- 真实 500 根快照重新计算为 3 条线段，独立参考 3 / 3 一致；
- 首段变化后的线段中枢和买卖点全部重新计算并通过不变量。

该检查能排除当前窗口内可证明错误的候选，但完整历史状态仍是彻底解决交易所
历史左边界不确定性的长期方案。

## 6. 有缺口等待状态的两阶段修复

### 6.1 等待期间端点迁移

v0.10.6 已修复第一层问题：有缺口主分型成立后，等待反向确认期间不能锁死
最早端点。更高顶或更低底出现时，端点必须迁移，并从新端点重启反向特征
序列。独立保护为：

```text
GAP_ENDPOINT_SUPERSEDED_BEFORE_CONFIRMATION
```

任何按缺口反向分型确认的线段，在确认前不得存在比最终端点更极端的同类笔
端点。

### 6.2 后续无缺口主分型必须继续参与竞争

v0.10.7 虽然允许端点迁移，但仍把状态固定在“缺口分支”。生产代码已经完整
回放出后续 `valid_confirmed` 主分型，却没有在等待循环中处理它们。

确定性回归：

```text
扫描起点：18
21：有缺口主分型，检测位置 23
25：无缺口主分型，检测位置 27
31：无缺口主分型，检测位置 33
```

旧结果：

```text
18 → 31
GAP_REVERSE_FRACTAL
确认位置 36
```

正确结果：

```text
18 → 25
NO_GAP
确认位置 27
```

修复后的等待循环在每个笔位置同时处理：

1. 严格早于当前位置的反向确认；
2. 当前同类笔带来的端点迁移；
3. 当前检测完成的主特征分型；
4. 与当前位置同时完成的反向确认。

若第 3 步出现有效无缺口主分型，它拥有同一时刻优先权并立即结束线段。只有
不存在更早有效无缺口主分型时，才允许使用 `GAP_REVERSE_FRACTAL`。

新增独立保护：

```text
GAP_CONFIRMATION_SKIPPED_EARLIER_NO_GAP_PRIMARY
```

验证器会从该线段起点重新独立回放主特征序列；若在缺口反向确认之前发现一
个仍是当时有效极值的无缺口主分型，测试直接失败。该保护能够识别旧版
`18 → 31` 结果，即使生产实现和参考实现再次犯同样错误也不能通过。

5000 根回归最终结果：

```text
已确认笔：338
已确认线段：57
特征元素：659
特征分型：88
未完成线段区域：3 笔
特征序列扫描位置：337 / 337
生产实现与独立事件回放：57 / 57
确认依据：57 / 57
缺口端点极值问题：0
跳过更早无缺口主分型问题：0
```

专项测试包括：

- `18 → 25 / NO_GAP / 27` 精确回归；
- 起点 303 在端点迁移后由后续无缺口主分型结束；
- 人工构造旧版错误证据，验证独立保护必然报错；
- 1000 组随机笔链生产实现 / 独立回放差分；
- 按笔前缀稳定性与防未来确认；
- 全部下游中枢和买卖点重新计算并执行不变量校验。

v0.10.5 的连续扫描修复、v0.10.6 的端点迁移修复和 v0.10.7 的首段边界
校验均继续保留。

## 7. 虚线语义

未确认笔：

- 从最后一条已确认笔终点开始；
- 方向与上一笔相反；
- 连接到当前未完成无包含 K 区域的最极端价；
- 尚未满足完整分型和最小笔长。

未确认线段：

- 从最后一条已确认线段终点开始；
- 使用未完成笔区间中的候选极值；
- 标准特征序列尚未完成无缺口或有缺口确认；
- 可能随新 K 延长、迁移或消失。

图层样式自动化断言：

```text
默认笔线宽：1.1
默认线段线宽：2.2
确认笔 / 未确认笔：颜色和粗细完全一致，仅 solid / dash 不同
确认线段 / 未确认线段：颜色和粗细完全一致，仅 solid / dash 不同
笔中枢 / 线段中枢：自定义边框颜色、粗细和填充透明度正确进入 Plotly shape
顶底分型：自定义颜色、大小和边框正确进入 marker
六类买卖点：每类自定义颜色和大小正确进入 marker
无效颜色、零线宽等配置：数据类直接拒绝
```

## 7. 图层样式配置验证

本次只修改展示层，不修改 K 线、分型、笔、线段、中枢和买卖点数据。验证采用同一份分析结果分别传入默认样式和自定义样式：

- 笔、线段的 `line.color / line.width / marker.size` 与配置逐项一致；
- 未确认笔和未确认线段自动继承对应实线颜色、宽度和端点大小，只把 `dash` 改为虚线；
- 两类中枢的矩形边框、填充 RGBA 和中轴标记均与配置一致；
- 顶底分型和六类买卖点的颜色、大小、边框逐项一致；
- 改变样式不会改变任何结构对象数量、端点、确认时间或验证结果。

默认值由原来的较粗显示调整为：

```text
笔：#EAB308，线宽 1.1，端点 5.0
线段：#7E22CE，线宽 2.2，端点 6.5
笔中枢边框：1.2，填充透明度 0.12
线段中枢边框：1.5，填充透明度 0.10
```

## 8. 旧 CZSC 基线的解释

冻结的 CZSC v0.9.69 仍用于去包含 K 和分型差分。

笔层不要求与旧状态机逐笔完全一致，因为项目已经根据人工发现的真实案例修复了共享端点迁移：同类更低底或更高顶出现后，旧共享端点必须回退并替换。现有测试明确验证这项差异是预期行为。

线段层使用独立标准特征序列实现逐项复算，避免把旧版局部极值简化算法当成正确基线。

## 9. 全屏比例与头部布局回归

此前头部使用 `y=1.33 / 1.22` 的纸面坐标，并预留 `230px` 顶部边距；在全屏视图中，纸面坐标会按主绘图区高度继续放大，形成明显的大块空白。

修复后：

```text
顶部边距：150px
标题位置：1.165
来源信息位置：1.105
状态条位置：1.012
来源信息行数：最多 4 行
Plotly autosize：开启
```

自动化断言：

- `layout.margin.t <= 160`；
- 标题注释不高于 `1.18`；
- 来源信息注释不高于 `1.12`；
- 来源信息最多 4 行；
- 首根、末根、数据源、市场、币种、周期、中枢和买卖点口径仍完整保留。

5000 根静态回归页面已重新导出，并确认生成的 Plotly 布局包含紧凑参数。

## 10. 自动化测试结果

```text
91 passed, 1 skipped
```

跳过项：未安装可选外部 `czsc` 包。

已执行的关键测试包括：

- 5000 根现货分页；
- 5000 + 1 快照；
- 增量刷新、去重、窗口滚动；
- 长暂停后的完整重载；
- 当前 K 与确认结构隔离；
- 确认结果缓存与收盘触发重算；
- 5000 根完整结构确定性；
- 标准特征序列独立差分；
- 笔、线段、双层中枢、六类买卖点回归；
- Streamlit 组件 key；
- 实线和虚线图层；
- 所有非 K 线图层的自定义样式传递、默认细线及非法配置拒绝。

## 10. 产物

```text
artifacts/regression/live_5000_5m_demo_raw.html
artifacts/regression/live_5000_5m_demo_merged.html
artifacts/regression/live_5000_5m_demo_closed_bars.csv
artifacts/regression/live_5000_5m_demo_current_bar.csv
artifacts/regression/live_5000_5m_demo_confirmed_strokes.csv
artifacts/regression/live_5000_5m_demo_provisional_strokes.csv
artifacts/regression/live_5000_5m_demo_confirmed_segments.csv
artifacts/regression/live_5000_5m_demo_segment_evidence.csv
artifacts/regression/live_5000_5m_demo_feature_elements.csv
artifacts/regression/live_5000_5m_demo_feature_fractals.csv
artifacts/regression/live_5000_5m_demo_unfinished_segment_strokes.csv
artifacts/regression/feature_sequence_stop_bug_summary.json
artifacts/regression/live_5000_5m_demo_provisional_segments.csv
artifacts/regression/live_5000_5m_demo_summary.json
```

所有静态回归图均有 `DEMO / 模拟数据` 水印。真实数据由运行中的 Streamlit 页面直接从 Binance 获取。

## 8. 悬停数据框配置验证

新增全局 hover 配置，覆盖 K 线、成交量、分型、笔、线段、中枢和买卖点：

```text
显示悬停数据框：可开关
背景颜色：#RRGGBB
背景不透明度：0 ~ 1
文字颜色：按背景亮度自动选择深色或浅色
```

自动化断言：

- 开启时使用 `x unified`，保持同一时间点的数据聚合；
- 背景色与不透明度正确转换成 Plotly RGBA；
- 深色背景自动使用浅色文字；
- 关闭时 `hovermode=False` 且 `hoverdistance=-1`，不再弹出遮挡图表的数据框；
- 控件拥有显式且唯一的 Streamlit key；
- hover 样式变化不影响任何结构计算。


## 11. 可回撤末笔不能确认实线段

专项复现使用同一确定性 5m 原始 K 序列：

```text
前 235 根 K：21 笔
候选线段确认位置：20（当前最后一笔）
前 236 根 K：最后一笔撤销，笔数 21 → 20
```

修复前，依赖位置 20 的线段会先作为实线出现，下一根 K 到来后消失。修复后：

```text
235 根 K 已确认线段：1→4、4→11
236 根 K 已确认线段：1→4、4→11
两次已确认线段签名完全一致
```

数据层新增硬约束与诊断码：

```text
SEGMENT_CONFIRMATION_USES_REVERSIBLE_LAST_STROKE
confirmed_at_position < len(strokes) - 1
```

验证不再只切割最终笔链，而是直接运行 `analyze_bars(bars[:235])` 与 `analyze_bars(bars[:236])`，并对 220～270 根原始 K 的连续前缀执行单调稳定性检查。

5000 根回归更新为：57 条已确认线段、1 条未确认线段虚线、659 个特征元素、88 个特征分型，生产实现与独立事件回放 57/57 一致。
